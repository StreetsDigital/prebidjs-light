export interface ReportFiltersProps {
  onFilterChange?: (filters: any) => void;
}

export function ReportFilters({ onFilterChange }: ReportFiltersProps) {
  // Placeholder component for future filter functionality
  // This can be expanded to include advanced filtering options like:
  // - Date range picker
  // - Bidder filters
  // - Ad unit filters
  // - Device type filters
  // - Domain filters

  return null;
}
